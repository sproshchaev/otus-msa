# Слайд 21 — exec-форма записи (JSON-массив).
# Программа запускается напрямую, без прослойки shell.
# Внутри контейнера один процесс: ping сразу с PID 1.
# Сигнал остановки доходит до процесса мгновенно -> docker stop завершает контейнер сразу.
FROM alpine:3.20

CMD ["ping", "127.0.0.1"]

# Слайд 20 — только ENTRYPOINT.
# Программа из ENTRYPOINT остаётся всегда; аргументы команды запуска
# не заменяют её, а дописываются к ней.
FROM alpine:3.20

ENTRYPOINT ["echo", "привет из ENTRYPOINT"]

# Слайды 18, 22 — отдельный RUN rm НЕ уменьшает размер образа.
# Файл создаётся в одном слое, удаляется в другом.
# Слой с большим файлом остаётся в истории образа, размер не падает.
FROM alpine:3.20

RUN dd if=/dev/zero of=/bigfile bs=1M count=50
RUN rm -f /bigfile

CMD ["echo", "layers-bad"]

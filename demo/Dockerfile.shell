# Слайд 21 — shell-форма записи.
# Команда запускается через /bin/sh -c "...".
# Внутри контейнера два процесса: /bin/sh (PID 1) и ping как дочерний.
# Из-за прослойки sh сигнал остановки доходит до ping не сразу -> docker stop ждёт таймаут.
FROM alpine:3.20

CMD ping 127.0.0.1

# Слайд 20 — только CMD.
# Значение по умолчанию, которое свободно заменяется аргументом команды запуска:
#   docker run --rm demo-cmd                       -> печатает строку из CMD
#   docker run --rm demo-cmd echo "другой текст"   -> CMD заменён целиком
FROM alpine:3.20

CMD ["echo", "привет из CMD"]

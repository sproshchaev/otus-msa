version: '3.8'

services:
  redis:
    image: redis:6.2-alpine
    container_name: redis-container
    ports:
      - "6379:6379"
    networks:
      - redis-network

networks:
  redis-network:
    driver: bridge
